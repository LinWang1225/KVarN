diff --git a/vllm/v1/attention/backends/kvarn_attn.py b/vllm/v1/attention/backends/kvarn_attn.py
--- a/vllm/v1/attention/backends/kvarn_attn.py
+++ b/vllm/v1/attention/backends/kvarn_attn.py
@@ -1740,6 +1740,21 @@ class KVarNAttentionImpl(AttentionImpl["KVarNMetadata"]):
             attn_out = self._mixed_batch_path(
                 q, key[:N], value[:N], kv_cache, attn_metadata
             )
+        # Benchmark-only Figure 5 probe. Normal serving never imports or calls
+        # the probe because the environment variable is absent.
+        if os.environ.get("VLLM_FIGURE5_PROBE_OUTPUT"):
+            from vllm.v1.attention.figure5_probe import probe_attention_output
+
+            attn_out = probe_attention_output(
+                method="kvarn",
+                impl=self,
+                layer=layer,
+                query=q,
+                key=key[:N].view(N, self.num_kv_heads, self.head_size),
+                value=value[:N].view(N, self.num_kv_heads, self.head_size),
+                attn_metadata=attn_metadata,
+                native_output=attn_out,
+            )
         if output.ndim == 3:
             output[:N] = attn_out.to(output.dtype)
         else:
diff --git a/vllm/v1/attention/backends/turboquant_attn.py b/vllm/v1/attention/backends/turboquant_attn.py
--- a/vllm/v1/attention/backends/turboquant_attn.py
+++ b/vllm/v1/attention/backends/turboquant_attn.py
@@ -15,6 +15,7 @@ Per-head per-position slot layout:
 
 import functools
 import math
+import os
 from dataclasses import dataclass
 from typing import Any, ClassVar
 
@@ -482,6 +483,21 @@ class TurboQuantAttentionImpl(AttentionImpl["TurboQuantMetadata"]):
                 PiT,
                 layer=layer,
             )
+        # Benchmark-only Figure 5 probe. Normal serving never imports or calls
+        # the probe because the environment variable is absent.
+        if os.environ.get("VLLM_FIGURE5_PROBE_OUTPUT"):
+            from vllm.v1.attention.figure5_probe import probe_attention_output
+
+            attn_out = probe_attention_output(
+                method="turboquant",
+                impl=self,
+                layer=layer,
+                query=q,
+                key=key[:N].view(N, self.num_kv_heads, self.head_size),
+                value=value[:N].view(N, self.num_kv_heads, self.head_size),
+                attn_metadata=attn_metadata,
+                native_output=attn_out,
+            )
         # Write into output buffer: attn_out is (N, Hq, D)
         # output may be 2D (N, Hq*D) or 3D (N, Hq, D)
         if output.ndim == 3:
